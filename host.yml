---
datacenter:
  children:
    routers:
      hosts:
        10.1.1.10:
      vars:
        testvars: 999
  vars:
    ansible_connection : network_cli
    ansible_network_os : ios
    ansible_user : cisco
    ansible_ssh_pass : cisco
    ntp_servers:
      -10.30.11.10
      -10.20.11.10


